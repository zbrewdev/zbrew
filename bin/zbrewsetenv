#
# zbrewsetenv: set up environment for configuration scripts
#
#set -x

if [ $# != 1 ]; then
	echo "Syntax: . zbrewsetenv <zbrew root directory>" >&2
	return 4
fi
zbrewroot="$1"

out=$(whence zbrewfuncs >/dev/null)
if [ $? -eq 0 ]; then
	. zbrewfuncs
else
    	echo "zbrew tools need to be in your PATH"
	return 4
fi
props="${zbrewroot}/zbrewglobalprops.json"
zbrewpropse zbrew config "${props}"

